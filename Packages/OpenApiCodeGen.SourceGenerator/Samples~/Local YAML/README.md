# Local YAML sample

This sample is the YAML form of the minimal OpenAPI 3.1 `Items API`.

Use the same steps as the [Local JSON sample](../Local%20JSON/README.md),
selecting `openapi.yaml` in the Generator window. The bounded YAML parser
normalizes this document into the same semantic pipeline as the JSON sample;
the generated client contract is equivalent.
