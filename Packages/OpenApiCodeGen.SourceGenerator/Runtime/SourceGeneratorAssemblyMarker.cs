namespace Rhycol.OpenApiCodeGen.SourceGenerator
{
    /// <summary>
    /// Keeps the analyzer owner assembly concrete so asmdef references define a stable analyzer scope.
    /// </summary>
    internal static class SourceGeneratorAssemblyMarker
    {
    }
}
